# BlackBox Confections — Frontend (React + Vite + Tailwind)

## Setup
```bash
cd frontend
npm install
```

## Configure API Base (optional)
Create `frontend/.env`:
```bash
VITE_API_BASE=http://localhost:8000/api
```

## Run
```bash
npm run dev
```
